29/11/2025
Muda nas configurações globais do git o nome da branch de master (que é o padrão, mas em desuso) para main.
git config --global init.defaultBranch main

Inicia um projeto git em um diretório
git init

Inicialização do git, basicamente identifica o usuário que fará as alterações.
git config --global user.name "Aguinaldo da Silva Sobrinho"
git config --global user.email "aguinaldo.sis@gmail.com"

Adiciona um arquivo para ser comitado
git add .     -> adiciona todos os arquivos do diretorio atual
git add <nome do arquivo>     -> adiciona um arquivo específico.
git add projeto-*  -> adiciona todos os arquivos que comecem com "projeto-"
git add <nome da diretorio>  -> adiciona o diretorio e todos os arquivos que estão dentro dele.


Mostra o status do projeot
git status

Mostra as alterações que ocorreram no arquivo desde o ultimo add
git diff <nome do arquivo>

Situação: seu eu criar um arquivo no projeto, executar o git add no arquivo, apagar o arquivo e depois executar um git commit do projeto, o arquivo eu criei será commitado mesmo que que tenha apagado ele.
para que isso não ocorra eu preciso, antes de commitar, executar o git rm <nome do arquivo>.

Remover um arquivo que esta sendo rastreado após o git add
git rm --cached <nome do arquivo>

git rm -r --cached <nome da pasta>

Mostra o histórico dos commits
git log -> mostra todos os commits que foram realizados
git log -p -> mostra o que foi alterado em casa commit
git log -p -2 -> mostra o que foi alterado nos ultimos dois commits
git log -p -1 <hash do commit> -> mostra o que foi alterado em um commit específico
git show <hash do commt>  -> mostra o que foi alterado em um commit específico

06/12/2025
Configuração de um repositorio remoto no git, usando o github como rempositorio.</br>
Primeiro passo é criar um conta no github e criar um repositório público.
Por questões de seguranção o Gihub só permite login via Token ou SSD, nesse caso vamos usar o SSH.

Após a conta criada no GitHub vamos cria as chaves com o e-mail configurado no git e envar para o github.
Execute os comandos extamente na ordem em que estão:

ssh-keygen -t ed25519 -C "aguinaldo.sis@gmail.com"
cat ~/.ssh/id_ed25519.pub 
ssh -T git@github.com

Após o comando cat, vai mostra o código da chave pública no terminal, copie esse código e acesse a seguinte configuração no GitHub:
settings > SSH and GPG keys

Clique em New SSH Keys e preencha com o conteúdo do cat.

Pronto, feito isso, agora vamos configurar um repositório remoto no Git, para isso execute os seguintes comandos:

git remote add origin git@github.com:aguinaldosis-infra/curso_git_github_ufu-2.git
git branch -M main
git push origin main

O endereço "git@github.com:aguinaldosis-infra/curso_git_github_ufu-2.git" a gente consegue quando cria o respositório ou depois do repositório criado atravez do botão <>Code

Branchs

Branchs são linhas de desenvolvimente em aparelo.

para criar uma nova branch:
 git branch <nome da branch>
 git branch Aguinaldo
 git branch Mirian
No exemplo anterior criamos duas branchs, uma com o nome Aguinaldo e outra com o nome Mírian. Dessa forma dois desenvolvedores podem conduzir o desenvolvimento em paralelo gerando códigos em diferentes funções do projeto.
após todas as alterações serem comitadas e feito o push para o rempositorio, o encarregado do projeto deve juntar essas alterações na branch principal, a main.
Para juntar as branchs do desenvolvedores na main utiliza-se os seguintes comandos:
 git merge Aguinalado main --->> mescla a branch do Aguinaldo
 git merge Mirian main ---->> mescla a branch da Mirian
Valer alertar que o comando deve ser executado na branch main, para que as alterações sejam adicionadas.
Situação hipotética, imagine que a Mirian queira mesclas as alterações que o Aguinaldo fez em sua branch, para isso ela vai executar os seguintes comandos:
 git checkout Mirian ---> entra na brach dela
 git merge Aguinaldo Mirian ---> mescla todas as alterações que o Aguinaldo fez.
Claro que para isso funcionar o Aguinaldo tem que ter feito o push para o repositório e a Mirian tem que ter feito o clone do repositorio após esse push 
















